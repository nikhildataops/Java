# ☕ Java Learning & Practice

This repository contains my **Java programming learning journey and practice programs**. I am building my Java fundamentals step by step, starting from the basics and gradually moving toward advanced concepts and **Data Structures & Algorithms (DSA)**.
## 📚 Topics Learned
### 1. Hello World
- Java program structure
- `main()` method
- `System.out.println()`
- Compiling and running a Java program
### 2. Comments
- Single-line comments `//`
- Multi-line comments `/* */`
- Documentation comments `/** */`
### 3. Variables
- Variable declaration
- Variable initialization
- Naming rules
- Local and class variables
- Default values of variables
### 4. Data Types
- Primitive data types
  - `byte`
  - `short`
  - `int`
  - `long`
  - `float`
  - `double`
  - `char`
  - `boolean`
- Reference data types
### 5. Concatenation
- String concatenation using `+`
- Combining strings with variables
- Combining different data types
### 6. Type Casting
- Implicit/Widening type casting
- Explicit/Narrowing type casting
- Converting values between compatible data types
### 7. Keywords
- Understanding Java reserved keywords
- Common keywords such as:
  - `class`
  - `public`
  - `static`
  - `void`
  - `int`
  - `char`
  - `boolean`
  - `new`
  - `return`
### 8. Escape Sequences in Java
- Purpose of escape sequences in strings
- Common escape sequences:
  - `\n` – newline
  - `\t` – tab
  - `\\` – backslash
  - `\"` – double quote
  - `\'` – single quote
  - `\r` – carriage return
### 9. Operators
- Arithmetic operators (`+`, `-`, `*`, `/`, `%`)
- Relational operators (`==`, `!=`, `>`, `<`, `>=`, `<=`)
- Logical operators (`&&`, `||`, `!`)
- Assignment operators (`=`, `+=`, `-=`, `*=`, `/=`)
- Operator precedence basics
### 10. Unary Operators
- Unary plus (`+`) and minus (`-`)
- Increment (`++`) – pre and post
- Decrement (`--`) – pre and post
- Logical complement (`!`)
### 11. Taking User Input
- Using the `Scanner` class
- Reading `int`, `double`, `String`, and other data types
- `nextInt()`, `nextDouble()`, `next()`, `nextLine()`
### 12. Add Two Numbers
- Reading two numbers from the user
- Performing addition
- Displaying the result
### 13. Swap Two Numbers
- Swapping using a temporary variable
- Swapping without a temporary variable (arithmetic method)
- Swapping using XOR (bitwise method)
### 14. Compound Interest
- Compound interest formula
- Taking principal, rate, and time as input
- Calculating and displaying the final amount and interest
### 15. Temperature Conversion (Fahrenheit to Celsius)
- Conversion formula: `C = (F - 32) * 5/9`
- Taking Fahrenheit value as input
- Displaying the converted Celsius value
### 16. Patterns using System.out.println (sout)
- Printing basic number/star patterns
- Using nested loops with `System.out.println()` and `System.out.print()`
- Common patterns: triangle, pyramid, and number patterns
> Learning Java one concept and one problem at a time. ☕💻
